{
  "kind": "collectionType",
  "collectionName": "services",
  "info": {
    "name": "services",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "name": {
      "type": "string"
    },
    "users": {
      "via": "services",
      "plugin": "users-permissions",
      "collection": "user"
    },
    "categories": {
      "model": "categories",
      "via": "services"
    },
    "request": {
      "via": "service_id",
      "collection": "request"
    }
  }
}
